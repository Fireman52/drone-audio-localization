# Drone Audio Localization

Drone localization system by audio signals from microphone array.

## Project Structure

- `src/` — sourse code
- `data/` — audio data
- `models/` — nn models

## Installing

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt